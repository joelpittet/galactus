(function ($, Drupal) {
  /*global jQuery:false */
  /*global Drupal:false */
  "use strict";

  /**
   * Provide vertical tab summaries for galactus settings.
   */
  Drupal.behaviors.galactusForms = {
    attach: function (context) {
    
      //$("input[type=checkbox], input[type=radio]").checkbox();
      
      //$("select").dropdown();

    }
  };

})(jQuery, Drupal);
